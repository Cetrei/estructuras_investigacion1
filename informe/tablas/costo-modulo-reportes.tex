{\small
\begin{longtable}{F{4cm} p{6cm} p{3cm} p{2.2cm}}
\toprule
\multicolumn{1}{p{4cm}}{Función} & Algoritmo & Costo real & Big O \\
\midrule
\endhead

calcularPromedioInstitucional &
    Recorre todos los registros sumando y al final divide entre el total &
    $n$ sumas\newline más $1$ división &
    $O(n)$ \\
\addlinespace

encontrarMayorPromedio &
    Recorre todos los registros llevando el máximo visto hasta el momento &
    $n$ comparaciones &
    $O(n)$ \\
\addlinespace

encontrarMayorCreditos &
    Recorre todos los registros llevando el máximo visto hasta el momento &
    $n$ comparaciones &
    $O(n)$ \\
\addlinespace

generarReporteEstadistico &
    Recorre todos los registros actualizando varios contadores a la vez (por carrera, por nivel, etc) &
    $n \times c$,\newline $c$ = cantidad de\newline categorías &
    $O(n)$ \\

\bottomrule
\end{longtable}
}
