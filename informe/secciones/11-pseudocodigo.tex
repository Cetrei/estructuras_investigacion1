% !TEX root = ../main.tex
\section{Pseudocódigo de las funciones principales}
\label{sec:pseudocodigo}

El pseudocódigo se escribe en sintaxis C simplificada: condicionales,
ciclos y llamadas a función explícitas.

\subsection{registrarEstudiante(datos)}
\input{pseudocodigo/registrar-estudiante}

Diagrama de flujo en la figura~\ref{fig:diagrama-registrarEstudiante} (sección \nameref{sec:diagramas}).

\subsection{buscarEstudiantePorCarne(carne)}
\input{pseudocodigo/buscar-estudiante-por-carne}

Diagrama de flujo en la figura~\ref{fig:diagrama-buscarEstudiantePorCarne} (sección \nameref{sec:diagramas}).

\subsection{buscarPosicionPorCarne(carne)}
\input{pseudocodigo/buscar-en-indice-primario}

Diagrama de flujo en la figura~\ref{fig:diagrama-buscarPosicionPorCarne} (sección \nameref{sec:diagramas}).

\subsection{buscarEstudiantesPorApellido(apellido)}
\input{pseudocodigo/buscar-estudiantes-por-apellido}

Diagrama de flujo en la figura~\ref{fig:diagrama-buscarEstudiantesPorApellido} (sección \nameref{sec:diagramas}).

\subsection{eliminarEstudianteLogico(carne)}
\input{pseudocodigo/eliminar-estudiante-logico}

Diagrama de flujo en la figura~\ref{fig:diagrama-eliminarEstudianteLogico} (sección \nameref{sec:diagramas}).

\subsection{registrarPosicionEnMapa(carne, posicion)}
\input{pseudocodigo/insertar-en-indice-primario}

Diagrama de flujo en la figura~\ref{fig:diagrama-registrarPosicionEnMapa} (sección \nameref{sec:diagramas}).

\subsection{fusionarRecienIngresados()}
\input{pseudocodigo/reorganizar-indice-primario}

Diagrama de flujo en la figura~\ref{fig:diagrama-fusionarRecienIngresados} (sección \nameref{sec:diagramas}).

\subsection{purgarInactivos()}
\input{pseudocodigo/purgar-inactivos}

Diagrama de flujo en la figura~\ref{fig:diagrama-purgarInactivos} (sección \nameref{sec:diagramas}).
