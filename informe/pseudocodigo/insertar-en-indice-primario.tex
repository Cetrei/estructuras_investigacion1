\begin{lstlisting}[style=pseudocodigo]
funcion registrarPosicionEnMapa(carne, posicion):
    agregarAlFinal(listaRecienIngresados, carne, posicion)

    si longitud(listaRecienIngresados) >= 0.10 * longitud(zonaOrdenada):
        fusionarRecienIngresados()
\end{lstlisting}
