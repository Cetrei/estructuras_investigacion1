\begin{lstlisting}[style=pseudocodigo]
funcion fusionarRecienIngresados():
    zonaOrdenada = mezclarOrdenado(zonaOrdenada, listaRecienIngresados)
    listaRecienIngresados = vacio()
\end{lstlisting}
